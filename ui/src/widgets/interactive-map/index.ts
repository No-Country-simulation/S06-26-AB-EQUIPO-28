// ---------------------------------------------------------------------------
// Public API — src/widgets/interactive-map
//
// Interactive map widget — MapLibre GL JS map showing antennas,
// concentration pins, and cluster region data.
// ---------------------------------------------------------------------------

export { InteractiveMapWidget } from "./ui/InteractiveMapWidget.tsx";

export type { Viewport } from "./model/useMapViewport.ts";
export type { MapDataState } from "./model/useMapData.ts";
